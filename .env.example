# Database connection string
DB_DSN="root:changeme@tcp(127.0.0.1:3306)/accounts?charset=utf8mb4&parseTime=True&loc=Local"

# Listen port
PORT=8080
# Listen address
HOST=0.0.0.0

# Log level (panic, fatal, error, warning, info, debug, trace)
LOG_LEVEL=info